/** Provides operations to manage the user singleton. */
export enum ManagedDeviceOwnerType {
    /** Unknown. */
    Unknown = "unknown",
    /** Owned by company. */
    Company = "company",
    /** Owned by person. */
    Personal = "personal",
}
