/** Provides operations to manage the user singleton. */
export enum Status {
    Active = "active",
    Updated = "updated",
    Deleted = "deleted",
    Ignored = "ignored",
    UnknownFutureValue = "unknownFutureValue",
}
